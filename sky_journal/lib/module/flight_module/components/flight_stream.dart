import 'package:flutter/material.dart';

class FlightStream extends StatelessWidget {
  const FlightStream({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
